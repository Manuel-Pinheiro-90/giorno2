import { Direttiva1Directive } from './direttiva1.directive';

describe('Direttiva1Directive', () => {
  it('should create an instance', () => {
    const directive = new Direttiva1Directive();
    expect(directive).toBeTruthy();
  });
});
